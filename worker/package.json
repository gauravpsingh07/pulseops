{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "test": "echo \"No worker tests configured yet\"",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260511.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.90.1"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
